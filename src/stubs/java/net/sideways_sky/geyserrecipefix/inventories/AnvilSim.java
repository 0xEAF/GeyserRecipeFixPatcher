package net.sideways_sky.geyserrecipefix.inventories;

import net.minecraft.world.inventory.AbstractContainerMenu;

/**
 * Compile-only stub - see Geyser_Recipe_Fix.java in this same source set
 * for why this exists. Declares only the one field (`menu`, inherited from
 * SimInventory in the real class) our injected AnvilRenameFix needs to
 * reference by name, so it links correctly against the real, official
 * AnvilSim.class at runtime. Never packaged.
 */
public class AnvilSim {
    public AbstractContainerMenu menu;
}
